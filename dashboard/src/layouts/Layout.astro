---
const { title = "Job Agent" } = Astro.props;
const path = Astro.url.pathname;
const nav = [
  ["/", "Overview"],
  ["/jobs", "Jobs"],
  ["/applications", "Applications"],
  ["/settings", "Settings"],
];
const isActive = (href: string) => (href === "/" ? path === "/" : path.startsWith(href));
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} · Job Agent</title>
  </head>
  <body>
    <header>
      <div class="topbar">
        <a class="brand" href="/"><span class="logo">◆</span> Job&nbsp;Agent</a>
        <nav>
          {nav.map(([href, label]) => (
            <a href={href} class={isActive(href) ? "active" : ""}>{label}</a>
          ))}
        </nav>
      </div>
    </header>
    <main><slot /></main>

    <style is:global>
      :root {
        /* Palette derived from temesgendev.vercel.app (neutral dark + sky accent) */
        --bg: #0a0a0a;
        --bg-2: #0f0f0f;
        --panel: #111111;
        --panel-2: #171717;
        --hover: #1c1c1c;
        --border: #262626;
        --border-2: #404040;
        --text: #e5e5e5;
        --text-strong: #fafafa;
        --muted: #737373;
        --faint: #585858;
        --accent: #7dd3fc;
        --accent-2: #bae6fd;
        --accent-dim: rgba(125, 211, 252, 0.13);
        --good: #4ade80;
        --warn: #fbbf24;
        --danger: #f87171;
        --info: #60a5fa;
        --radius: 12px;
        --radius-sm: 8px;
        --shadow: 0 1px 2px rgba(0,0,0,.4), 0 8px 24px rgba(0,0,0,.25);
        --font: ui-sans-serif, -apple-system, "Segoe UI", Roboto, Inter, system-ui, sans-serif;
        --mono: ui-monospace, "SF Mono", "JetBrains Mono", Menlo, monospace;
      }
      * { box-sizing: border-box; }
      html { color-scheme: dark; }
      body {
        margin: 0; background: radial-gradient(1100px 520px at 72% -12%, #161616 0, var(--bg) 58%);
        color: var(--text); font: 14.5px/1.55 var(--font); -webkit-font-smoothing: antialiased;
        letter-spacing: .005em; min-height: 100vh;
      }

      /* Header */
      header {
        position: sticky; top: 0; z-index: 10;
        background: rgba(10,12,16,.72); backdrop-filter: blur(12px);
        border-bottom: 1px solid var(--border);
      }
      .topbar { max-width: 1120px; margin: 0 auto; padding: 0 24px; height: 60px;
        display: flex; align-items: center; gap: 20px; }
      .brand { display: flex; align-items: center; gap: 8px; font-weight: 680; color: var(--text);
        text-decoration: none; font-size: 15px; padding-right: 20px; border-right: 1px solid var(--border); height: 30px; }
      .brand:hover { text-decoration: none; }
      .brand .logo { color: var(--accent); filter: drop-shadow(0 0 8px var(--accent-dim)); }
      nav { display: flex; gap: 2px; flex: 1; }
      nav a { position: relative; color: var(--muted); text-decoration: none; font-size: 14px; font-weight: 500;
        padding: 8px 14px; border-radius: 8px; transition: background .15s, color .15s; }
      nav a:hover { color: var(--text); background: var(--hover); text-decoration: none; }
      nav a.active { color: var(--text); background: var(--accent-dim); }
      nav a.active::after { content: ""; position: absolute; left: 14px; right: 14px; bottom: -1px; height: 2px;
        background: var(--accent); border-radius: 2px 2px 0 0; box-shadow: 0 0 8px var(--accent); }

      /* Layout */
      main { max-width: 1120px; margin: 0 auto; padding: 28px 24px 64px; }
      h1 { font-size: 22px; font-weight: 650; margin: 4px 0 20px; letter-spacing: -.01em; color: var(--text-strong); }
      a { color: var(--accent); text-decoration: none; }
      a:hover { color: var(--accent-2); text-decoration: underline; }
      .muted { color: var(--muted); }

      /* Panels & cards */
      .panel { background: var(--panel); border: 1px solid var(--border); border-radius: var(--radius);
        padding: 18px 20px; margin-bottom: 20px; box-shadow: var(--shadow); overflow-x: auto; }
      .panel > strong { font-size: 13px; text-transform: uppercase; letter-spacing: .06em; color: var(--muted); font-weight: 600; }
      .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 14px; margin-bottom: 20px; }
      .card { background: linear-gradient(180deg, var(--panel-2), var(--panel)); border: 1px solid var(--border);
        border-radius: var(--radius); padding: 16px 18px; transition: border-color .15s, transform .15s; }
      .card:hover { border-color: var(--border-2); transform: translateY(-1px); }
      .card .n { font-size: 28px; font-weight: 680; font-variant-numeric: tabular-nums;
        background: linear-gradient(180deg, #fafafa, var(--accent)); -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; }
      .card .l { color: var(--muted); font-size: 12.5px; margin-top: 2px; }

      /* Tables */
      table { width: 100%; border-collapse: collapse; font-size: 13.5px; }
      th, td { text-align: left; padding: 11px 12px; border-bottom: 1px solid var(--border); vertical-align: top; }
      th { color: var(--faint); font-weight: 600; font-size: 11.5px; text-transform: uppercase; letter-spacing: .06em; }
      tbody tr { transition: background .12s; }
      tbody tr:hover { background: var(--hover); }
      tbody tr:last-child td { border-bottom: none; }
      td a { font-weight: 550; }

      /* Score pill */
      .score { font-family: var(--mono); font-weight: 600; color: var(--accent-2);
        font-variant-numeric: tabular-nums; }

      /* Badges (semantic by status) */
      .badge { display: inline-block; padding: 3px 9px; border-radius: 999px; font-size: 11.5px;
        font-weight: 550; background: var(--panel-2); color: var(--muted); border: 1px solid var(--border-2); }
      .badge.submitted { background: rgba(63,185,80,.14); color: #5ad16b; border-color: rgba(63,185,80,.3); }
      .badge.interview { background: rgba(86,160,255,.14); color: var(--info); border-color: rgba(86,160,255,.3); }
      .badge.offer { background: rgba(227,160,8,.16); color: #f0b429; border-color: rgba(227,160,8,.32); }
      .badge.rejected, .badge.failed { background: rgba(248,81,73,.14); color: #ff6a60; border-color: rgba(248,81,73,.3); }
      .badge.awaiting_approval, .badge.drafting { background: rgba(227,160,8,.12); color: #e3a008; border-color: rgba(227,160,8,.28); }

      /* Forms */
      form.filters { display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
      label { display: inline-flex; gap: 6px; align-items: center; color: var(--muted); font-size: 13px; }
      select, input { background: var(--bg-2); color: var(--text); border: 1px solid var(--border-2);
        border-radius: var(--radius-sm); padding: 8px 11px; font: inherit; outline: none; transition: border-color .15s, box-shadow .15s; }
      select:focus, input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-dim); }
      button { background: var(--accent); color: #06222e; border: none; border-radius: var(--radius-sm);
        padding: 8px 14px; font: inherit; font-weight: 600; cursor: pointer; transition: filter .15s, transform .05s; }
      button:hover { filter: brightness(1.08); }
      button:active { transform: translateY(1px); }

      /* Bars / charts */
      .bar-track { background: var(--bg-2); border-radius: 999px; height: 8px; overflow: hidden; }
      .bar { height: 8px; border-radius: 999px; background: linear-gradient(90deg, var(--accent), var(--accent-2)); }

      pre { background: var(--bg-2); border: 1px solid var(--border); border-radius: var(--radius-sm);
        padding: 14px; overflow-x: auto; color: #c8cdd6; line-height: 1.6; }
      code { background: var(--bg-2); border: 1px solid var(--border); border-radius: 5px; padding: 1px 6px; font-family: var(--mono); font-size: .9em; }
    </style>
  </body>
</html>
